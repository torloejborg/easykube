apiVersion: v1
kind: Service
metadata:
  name: {{.DeploymentName}}-service
  labels:
    app: {{.DeploymentName}}
spec:
  type: ClusterIP
  ports:
    - port: 80
      protocol: TCP
  selector:
    app: {{.DeploymentName}}
