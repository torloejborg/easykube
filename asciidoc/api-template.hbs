
{{!-- Class (only real classes have kind "class" AND no params/properties) --}}
{{#each this}}
  {{#if constructor}}
= {{name}}

{{description}}

  {{/if}}
{{/each}}

== Methods / Properties

{{!-- Only instance methods (they always have memberof + params) --}}
{{#each this}}
  {{#if memberof}}
=== {{name}} [[{{name}}]]

{{description}}

[source,javascript]
----
{{name}}({{#each params}}{{name}}{{#unless @last}}, {{/unless}}{{/each}})
----

{{#if params.length}}
.Parameters
[cols="1,1,3"]
|===
| Name | Type | Description

{{#each params}}
| `{{name}}`
| {{#each type.names}}{{this}}{{#unless @last}}, {{/unless}}{{/each}}
| {{description}}
{{/each}}

|===

{{#if returns}}
.Returns

`{{#each returns.[0].type.names}}{{this}}{{/each}}` — {{returns.[0].description}}
{{/if}}

    {{/if}}
  {{/if}}
{{/each}}

== Types

{{!-- Typedefs (like HttpResponse, ExecResult) --}}
{{#each this}}
  {{#if properties}}
=== {{name}}

{{description}}

[cols="1,2,3"]
|===
| Name | Type | Description

{{#each properties}}
| `{{name}}`
| {{#each type.names}}{{this}}{{#unless @last}}, {{/unless}}{{/each}}
| {{description}}
{{/each}}

|===
  {{/if}}
{{/each}}

{{!-- Standalone callbacks (no memberof, but have params) --}}
{{#each this}}
  {{#unless memberof}}
    {{#if params}}
=== {{name}}

[source,javascript]
----
function({{#each params}}{{name}}: {{#each type.names}}{{this}}{{/each}}{{#unless @last}}, {{/unless}}{{/each}}){{#if returns}}: {{#each returns.[0].type.names}}{{this}}{{/each}}{{/if}}
----

{{description}}

    {{/if}}
  {{/unless}}
{{/each}}
